services:
  postgres:
    image: postgres:17-alpine
    container_name: modul555-dispatcher-db
    restart: unless-stopped
    environment:
      POSTGRES_DB: ${POSTGRES_DB:-modul555_dispatcher}
      POSTGRES_USER: ${POSTGRES_USER:-dispatcher}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD:-dispatcher}
    volumes:
      - dispatcher-pgdata:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U $$POSTGRES_USER -d $$POSTGRES_DB"]
      interval: 5s
      timeout: 5s
      retries: 15
    networks:
      - dispatcher

  api:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: modul555-dispatcher-api
    restart: unless-stopped
    environment:
      ASPNETCORE_ENVIRONMENT: Production
      ASPNETCORE_URLS: http://+:8080
      ConnectionStrings__Default: Host=postgres;Port=5432;Database=${POSTGRES_DB:-modul555_dispatcher};Username=${POSTGRES_USER:-dispatcher};Password=${POSTGRES_PASSWORD:-dispatcher}
      Keycloak__Authority: ${KEYCLOAK_AUTHORITY:-http://72.56.252.95:8080/realms/stroy-company}
      Keycloak__Audience: ${KEYCLOAK_AUDIENCE:-}
      Keycloak__TokenClientId: ${KEYCLOAK_TOKEN_CLIENT_ID:-admin-cli}
    depends_on:
      postgres:
        condition: service_healthy
    networks:
      - dispatcher

  web:
    build:
      context: ./web
      dockerfile: Dockerfile
    container_name: modul555-dispatcher-web
    restart: unless-stopped
    ports:
      - "${HTTP_PORT:-8080}:80"
    depends_on:
      - api
    networks:
      - dispatcher

volumes:
  dispatcher-pgdata:

networks:
  dispatcher:
    driver: bridge
